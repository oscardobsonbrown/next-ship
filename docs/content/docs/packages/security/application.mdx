---
title: Application Security
description: Security measures taken to protect your applications.
---

next-ship uses [Nosecone](https://docs.nosecone.com/) for security headers. You can extend this with your own security middleware as needed.

## Philosophy

Proper security protections need the full context of the application, which is why security rules and protections should be located alongside the code they are protecting.

Security as code means you can version control your security rules, track changes through pull requests, and test them locally before deploying to production.

## Configuration

The `@repo/security` package provides security headers via Nosecone. Both the `web` and `app` apps use these headers:

### Web

For the `web` app, security headers are configured in the Middleware and run on every request by default, except for static assets.

### App

For `app`, security headers are configured in the middleware for all routes. Clerk also includes bot detection and rate limiting in their login route handlers by default.

## Scaling up your security

You can add additional security middleware to `@repo/security` as needed. Consider implementing:

- **Rate limiting**: Use a library like `rate-limiter-flexible` or a service like Upstash Redis
- **Bot detection**: Implement your own bot detection or use a third-party service
- **WAF protection**: Consider Cloudflare, AWS WAF, or similar services for additional protection

### What about DDoS attacks?

Network layer attacks are usually generic and high volume, making them best handled by your hosting platform. Most cloud providers offer network DDoS protection as a default feature.

Volumetric network attacks are best handled by your hosting provider. Application level attacks need to be handled by the application.
